[package]
name = "capture"
version = "0.1.0"
edition.workspace = true

[dependencies]
shared = { path = "../shared" }
bytes = { workspace = true }
lazy_static = "1"
num_cpus = "1"

[target.'cfg(target_os = "macos")'.dependencies]
block = "0.1"

[build-dependencies]
bindgen = "0.70"
pkg-config = "0.3"
